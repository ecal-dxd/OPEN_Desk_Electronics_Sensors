{
	"name": "arduino_pong",
	"version": "1.0.0",
	"description": "A physical Pong game where two players control paddles using Arduino sensors. Player 1 uses a linear potentiometer; Player 2 uses an ultrasonic distance sensor.",
	"license": "ISC",
	"author": "",
	"type": "commonjs",
	"main": "index.js",
	"scripts": {
		"backend": "cd backend && npm start",
		"frontend": "cd frontend && npm run dev",
		"start": "npm run backend & npm run frontend",
		"closePorts": "lsof -ti:3000 | xargs kill",
		"test": "echo \"Error: no test specified\" && exit 1"
	}
}
